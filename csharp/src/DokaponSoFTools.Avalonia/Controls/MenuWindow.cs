using Avalonia.Controls.Primitives;

namespace DokaponSoFTools.App.Controls;

/// <summary>
/// The signature "Classic JRPG menu" container: an ornate command-window with a
/// dark outer frame, a light inner bevel, a royal-blue gradient fill, and an
/// optional white header bar. Used for every panel so the whole app reads as a
/// stack of RPG command windows. Styled by the ControlTheme in
/// Themes/ControlThemes.axaml.
/// </summary>
public class MenuWindow : HeaderedContentControl
{
}
