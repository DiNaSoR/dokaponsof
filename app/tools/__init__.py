"""
Bundled external tools for Dokapon SoF Tools.

This directory contains bundled executables for:
- FFmpeg (ffmpeg.exe, ffprobe.exe) - Video/audio conversion
- opusenc (opusenc.exe) - Opus audio encoding

Tool Structure:
    tools/
        ffmpeg/
            ffmpeg.exe
            ffprobe.exe
        opusenc/
            opusenc.exe

If tools are not bundled, the application will fall back to system PATH.
"""

